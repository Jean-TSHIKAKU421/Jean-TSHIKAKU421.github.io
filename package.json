{
  "name": "jean-tshikaku-portfolio",
  "version": "1.0.0",
  "description": "Portfolio - Jean TSHIKAKU",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "optimize-images": "node scripts/optimize-images.js",
    "build": "npm run optimize-images",
    "deploy": "npm run build && bash publier.sh"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "sharp": "^0.35.3"
  }
}